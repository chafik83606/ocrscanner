import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Test placeholder — l'app nécessite des providers pour démarrer.
    expect(true, isTrue);
  });
}
